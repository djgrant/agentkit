# Dependencies
node_modules/

# Package manager files (auto-generated)
package.json
# except the root pok CLI manifest
!/package.json
bun.lock
pnpm-lock.yaml
pnpm-workspace.yaml
package-lock.json
yarn.lock

# Runtime files
.screenshots/

# OS files
.DS_Store

# MCP secrets + per-target manifests (generated)
common/mcp/secrets.env
**/.agentkit-mcp.json
**/*.agentkit.bak

# Tooling caches
.ruff_cache/
__pycache__/

# Project-specific directories (should live in project's .opencode/)
work/
learnings/
