{
  "$comment": "Canonical MCP server topology, projected into every harness by `pok sync`. This is the ONE file you edit to add/remove servers. Secrets live as ${VAR} placeholders only — never paste raw credentials here. Real values go in common/mcp/secrets.env (gitignored). Optional per-server `targets` restricts which harnesses receive it (default: all).",
  "servers": {
    "context7": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp", "--api-key", "${CONTEXT7_API_KEY}"]
    },
    "github": {
      "transport": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": { "Authorization": "Bearer ${GITHUB_TOKEN}" }
    },
    "chrome-devtools": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest", "--autoConnect"],
      "targets": ["opencode", "amp"]
    }
  }
}
